---
const phases = [
  { number: '0', label: 'Onboarding', icon: 'user-plus' },
  { number: '1', label: 'The Launch', icon: 'rocket' },
  { number: '2', label: 'EMD & Disclosures', icon: 'file-text' },
  { number: '3', label: 'Investigation', icon: 'search' },
  { number: '4', label: 'Financing & Title', icon: 'landmark' },
  { number: '5', label: 'Pre-Closing', icon: 'clipboard' },
  { number: '6', label: 'Closing', icon: 'check-circle' },
];
---

<section class="relative overflow-hidden bg-gradient-to-br from-blue-50 via-violet-50/50 to-white py-20 md:py-28 lg:py-32">
  <div class="max-w-7xl mx-auto px-6 md:px-8">
    <div class="text-center max-w-3xl mx-auto">
      <p class="text-sm font-semibold uppercase tracking-wider text-blue-600 mb-3">Features</p>
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold tracking-tight text-slate-900">
        Your AI Transaction Coordinator,
        <span class="bg-gradient-to-r from-blue-600 to-violet-600 bg-clip-text text-transparent"> from Contract to Close</span>
      </h1>
      <p class="mt-6 text-lg text-slate-600 leading-relaxed max-w-2xl mx-auto">
        Britanni handles the seven phases of every residential real estate transaction — so you don't have to.
      </p>
    </div>

    <!-- Phase Timeline -->
    <div class="mt-16 relative">
      <!-- Connecting line -->
      <div class="hidden lg:block absolute top-6 left-0 right-0 h-0.5 bg-gradient-to-r from-blue-200 via-violet-200 to-blue-200" aria-hidden="true"></div>

      <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-7 gap-4 md:gap-3">
        {phases.map(({ number, label }, i) => (
          <div class="relative text-center group">
            <div class="relative mx-auto w-12 h-12 bg-white border-2 border-blue-200 group-hover:border-blue-500 rounded-full flex items-center justify-center mb-3 transition-colors z-10 shadow-sm">
              <span class="text-sm font-bold text-blue-600">{number}</span>
            </div>
            <div class="text-xs md:text-sm font-medium text-slate-600 group-hover:text-slate-900 transition-colors">{label}</div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
